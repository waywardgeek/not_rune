goal: statements
;

statements: -> ()
| statements statement NEWLINE -> $1.$2
;

statement: list
;

list: "(" values ")" -> $2
;

values: -> ()
| values value -> $1.$2
;

value: list | IDENT | INTEGER | FLOAT | BOOL | STRING | operator
;

operator: "+" | "-" | "*" | "/" | "%" | "==" | "!=" | ">" | "<" | ">=" | "<=" | "!" | "&&" | "||" | "~" | "&" | "|" | "^" | "<<" | ">>"
;
